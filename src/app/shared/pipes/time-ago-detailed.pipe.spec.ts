import { TimeAgoDetailedPipe } from './time-ago-detailed.pipe';

describe('TimeAgoDetailedPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoDetailedPipe();
    expect(pipe).toBeTruthy();
  });
});
